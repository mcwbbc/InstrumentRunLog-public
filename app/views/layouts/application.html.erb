<!DOCTYPE html>
<html>
<head>
  <title>InstrumentRunLog</title>
	<link rel="Shortcut Icon" href="/favicon.ico?v=3">
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
	<% if logged_in? %>
		<%= render 'menu/menu' %>
	<% end %>
	<div class="content">
		<%= yield %>
	</div>
	<% if logged_in? %>
		<%= render 'footer/scripts' %>
	<% end %>
</body>
</html>
