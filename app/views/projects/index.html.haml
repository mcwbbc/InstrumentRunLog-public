%h1 Projects

%span.action= link_to 'New Project', new_project_path

.data
	%table.info
		%tr.info
			%th.info Name
			%th.info Edit
		- @projects.each do |p|
			%tr{:class => cycle('infoOdd', 'infoEven')}
				%td.info= p.name
				%td.info= link_to 'Edit', edit_project_path(p), :class => 'edit'
